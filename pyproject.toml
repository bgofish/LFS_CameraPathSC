[project]
name = "CamPath_Json"
version = "0.2.3"
description = "Camera Json Path Creator - Circles & Spirals"

[tool.lichtfeld]
hot_reload = true
plugin_api = ">=1,<2"
lichtfeld_version = ">=0.5.1"
required_features = []
